<div class="fixed-top navigation my-custom-shadow">
  <nav class="navbar navbar-row my-md-2">
    <div class="container-fluid d-flex justify-content-around">

      <div class="dis-no">
        <a class="navbar-brand" href="/">
          <%= image_tag "helmet2.png", class:"logo-img" %>
        </a>
      </div>

      <div class="d-flex">
        <a class="navbar-brand" href="/"><h1 class="main-title text-decoration-underline">Gotham Promotion</h1></a>
      </div>

      <div class="dis-no">
        <div class=" icons-lang">
          <ul class="nav cont-lang-block mb-2 mb-md-0">
            <li>
              <i class="fa fa-whatsapp contact-icon" aria-hidden="true"></i>
            </li>
            <li>
              <i class="fa fa-instagram contact-icon"></i>
            </li>
            <li>
              <i class="fa fa-facebook-official contact-icon "></i>
            </li>
            <li>
                <i class="fa fa-map-marker contact-icon"></i>
            </li>
            <li>
              <i class="fa fa-envelope contact-icon"></i>
            </li>

            <li class="dropdown", id="lang-selector">
              <%= link_to '#', id: 'lang-selector', class: 'nav-link px-2 dropdown-toggle text-white',
                data: {"bs-toggle": 'dropdown'} do %>
                <%= tag.div '', class: "flag #{I18n.locale}-flag mt-1" %>
                <%= image_tag "#{locale}.png", class: "lang-icon" %>

                <%= t I18n.locale %>
              <% end %>

              <ul class="dropdown-menu lang-menu fs-4">
                <% I18n.available_locales.each do |locale| %>
                  <li>
                    <% if I18n.locale == locale %>
                      <span class="dropdown-item">
                        <%= image_tag "#{locale}.png", class: "lang-icon" %>
                        <%= t(I18n.locale) %>
                      </span>
                    <% else %>
                      <a href="<%= url_for(locale: locale) %>" class="dropdown-item">
                        <%= image_tag "#{locale}.png", class: "lang-icon" %>
                        <%= t(locale) %>
                      </a>
                    <% end %>
                  </li>
                <% end %>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </nav>

  <nav class="navbar navbar-expand-lg d-flex justify-content-center">

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <div class="shine-effect">
        <%= image_tag "helmet2.png", class:"logo-burger", alt:"Menu" %>
      </div>
    </button>

    <div class="collapse navbar-collapse justify-content-center" id="navbarNavDropdown">
      <ul class="navbar-nav rrr">
        <li class="nav-item">
          <%= link_to t("home"), "/main", class:"nav-link active text-white" %>
        </li>

        <li class="nav-item">
          <%= link_to t("about_us_title"), about_path, class:"nav-link active text-white" %>
        </li>

        <li class="nav-item dropdown my-dropdown">
          <%= link_to t("events"), events_path, class: "nav-link dropdown-toggle text-white", role: "button", "data-bs-toggle" => "dropdown", "aria-expanded" => "false" %>
          <div class="container">
          <ul class="dropdown-menu text-center">
            <% @last_events.each do |event| %>
              <li><%= link_to event.title, event_path(event), class: "dropdown-item fs-5" %></li>
            <% end %>
          </ul>
          </div>
        </li>

        <li class="nav-item">
          <%= link_to t("athletes"), fighters_path, class:"nav-link active text-white" %>
        </li>

        <li class="nav-item">
          <%= link_to t("shop"), shop_path, class:"nav-link active text-white" %>
        </li>

        <li class="nav-item">
          <%= link_to t("blog"), blog_path, class:"nav-link active text-white" %>
        </li>

        <li class="nav-item dropdown my-dropdown dis-no-desktop">
          <%= content_tag :span, t("contact_us"), class: "nav-link dropdown-toggle text-white", role: "button", "data-bs-toggle" => "dropdown", "aria-expanded" => "false" %>
          <div class="container">
            <ul class="dropdown-menu text-center ">
              <div class="container d-flex flex-row justify-content-around">
                <li>
                  <i class="fa fa-whatsapp contact-icon fs-4" aria-hidden="true"></i>
                </li>
                <li>
                  <i class="fa fa-instagram contact-icon fs-4"></i>
                </li>
                <li>
                  <i class="fa fa-facebook-official contact-icon fs-4"></i>
                </li>
                <li>
                    <i class="fa fa-map-marker contact-icon fs-4"></i>
                </li>
                <li>
                  <i class="fa fa-envelope contact-icon fs-4"></i>
                </li>
              </div>
            </ul>
          </div>
        </li>

        <li class="dropdown d-flex justify-content-center flex-column text-center dis-no-desktop">
          <a href="#" class="nav-link px-2 dropdown-toggle text-white" role="button" id="lang-selector" data-bs-toggle="dropdown" aria-expanded="false">
            <%= tag.div '', class: "flag #{I18n.locale}-flag mt-1" %>
            <%= image_tag "#{I18n.locale}.png", class: "lang-icon" %>
            <%= t I18n.locale %>
          </a>

          <ul class="dropdown-menu lang-menu fs-4">
            <% I18n.available_locales.each do |locale| %>
              <li>
                <% if I18n.locale == locale %>
                  <span class="dropdown-item">
                    <%= image_tag "#{locale}.png", class: "lang-icon" %>
                    <%= t(I18n.locale) %>
                  </span>
                <% else %>
                  <a href="<%= url_for(locale: locale) %>" class="dropdown-item">
                    <%= image_tag "#{locale}.png", class: "lang-icon" %>
                    <%= t(locale) %>
                  </a>
                <% end %>
              </li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </nav>
</div>
